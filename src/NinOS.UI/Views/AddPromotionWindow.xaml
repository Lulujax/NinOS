<Window x:Class="NinOS.UI.Views.AddPromotionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Añadir Promoción" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="120" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0" SelectedIndex="{Binding promo_type_index}" Margin="0,0,0,15" Padding="5">
            <ComboBoxItem Content="Producto Individual en Oferta" />
            <ComboBoxItem Content="Armar Nuevo Kit" />
            <ComboBoxItem Content="Armar Nuevo Combo" />
        </ComboBox>

        <TextBlock Grid.Row="1" Text="Buscar Producto:" FontWeight="Bold" Margin="0,0,0,5" />
        <TextBox Grid.Row="2" VerticalAlignment="Top" Text="{Binding promo_search_query, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,5" />
        
        <ListBox Grid.Row="2" ItemsSource="{Binding promo_search_results}" SelectedItem="{Binding selected_promo_product}" Margin="0,35,0,10" Background="#F9F9F9">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center">
                            <Run Text="{Binding product_code}" FontWeight="Bold" />
                            <Run Text=" - " />
                            <Run Text="{Binding name}" />
                        </TextBlock>
                        <Button Grid.Column="1" Content="Agregar" Command="{Binding DataContext.add_to_builder_command, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Padding="10,2" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.promo_type_index, RelativeSource={RelativeSource AncestorType=Window}}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <DataGrid Grid.Row="3" ItemsSource="{Binding builder_items}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,15">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding promo_type_index}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding product_ref.product_code}" IsReadOnly="True" Width="80" />
                <DataGridTextColumn Header="Producto" Binding="{Binding product_ref.name}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Cant." Binding="{Binding quantity, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Command="{Binding DataContext.remove_from_builder_command, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="#FFCDD2" Cursor="Hand" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding promo_type_index}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="Nombre del Kit / Combo" FontWeight="Bold" Margin="0,0,0,5" />
            <TextBox Text="{Binding new_promo_name, UpdateSourceTrigger=PropertyChanged}" Padding="5" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,0,0,15">
            <TextBlock Text="Precio Total Promocional ($)" FontWeight="Bold" Margin="0,0,0,5" />
            <TextBox Text="{Binding new_promo_price, UpdateSourceTrigger=PropertyChanged}" Padding="5" />
        </StackPanel>

        <Button Grid.Row="6" Content="Guardar" Command="{Binding save_promotion_command}" Background="#0078D7" Foreground="White" FontWeight="Bold" Padding="10" Cursor="Hand" />
    </Grid>
</Window>