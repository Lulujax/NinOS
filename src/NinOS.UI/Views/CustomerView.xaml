<UserControl x:Class="NinOS.UI.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Gestión de Clientes" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" FontWeight="Bold" Margin="0,0,0,5"/>
        
        <TextBlock Grid.Row="2" Text="{Binding AllCustomers.Count, StringFormat='Total Clientes: {0}'}" FontSize="16" Margin="0,0,0,5"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Buscar" Command="{Binding LoadCustomersCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Agregar Cliente" Command="{Binding AddCustomerCommand}" Background="#0078D7" Foreground="White" Padding="10,5"/>
        </StackPanel>
        
        <TabControl Grid.Row="4" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="Todos">
                <ListView ItemsSource="{Binding AllCustomers}" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Código" DisplayMemberBinding="{Binding CustomerCode}" Width="100"/>
                            <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding BusinessName}" Width="200"/>
                            <GridViewColumn Header="RIF" DisplayMemberBinding="{Binding Rif}" Width="120"/>
                            <GridViewColumn Header="Contacto" DisplayMemberBinding="{Binding ContactName}" Width="150"/>
                            <GridViewColumn Header="Teléfono" DisplayMemberBinding="{Binding PhoneNumber}" Width="120"/>
                            <GridViewColumn Header="Vendedor" DisplayMemberBinding="{Binding SellerName}" Width="100"/>
                            <GridViewColumn Header="Acciones" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Editar" Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                            <Button Content="Eliminar" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Background="#DC3545" Foreground="White" Padding="5,2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Juan">
                <ListView ItemsSource="{Binding JuanCustomers}" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Código" DisplayMemberBinding="{Binding CustomerCode}" Width="100"/>
                            <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding BusinessName}" Width="200"/>
                            <GridViewColumn Header="RIF" DisplayMemberBinding="{Binding Rif}" Width="120"/>
                            <GridViewColumn Header="Contacto" DisplayMemberBinding="{Binding ContactName}" Width="150"/>
                            <GridViewColumn Header="Teléfono" DisplayMemberBinding="{Binding PhoneNumber}" Width="120"/>
                            <GridViewColumn Header="Acciones" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Editar" Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                            <Button Content="Eliminar" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Background="#DC3545" Foreground="White" Padding="5,2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Sandra">
                <ListView ItemsSource="{Binding SandraCustomers}" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Código" DisplayMemberBinding="{Binding CustomerCode}" Width="100"/>
                            <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding BusinessName}" Width="200"/>
                            <GridViewColumn Header="RIF" DisplayMemberBinding="{Binding Rif}" Width="120"/>
                            <GridViewColumn Header="Contacto" DisplayMemberBinding="{Binding ContactName}" Width="150"/>
                            <GridViewColumn Header="Teléfono" DisplayMemberBinding="{Binding PhoneNumber}" Width="120"/>
                            <GridViewColumn Header="Acciones" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Editar" Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                            <Button Content="Eliminar" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Background="#DC3545" Foreground="White" Padding="5,2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Anais">
                <ListView ItemsSource="{Binding AnaisCustomers}" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Código" DisplayMemberBinding="{Binding CustomerCode}" Width="100"/>
                            <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding BusinessName}" Width="200"/>
                            <GridViewColumn Header="RIF" DisplayMemberBinding="{Binding Rif}" Width="120"/>
                            <GridViewColumn Header="Contacto" DisplayMemberBinding="{Binding ContactName}" Width="150"/>
                            <GridViewColumn Header="Teléfono" DisplayMemberBinding="{Binding PhoneNumber}" Width="120"/>
                            <GridViewColumn Header="Acciones" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Editar" Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                            <Button Content="Eliminar" Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}" Background="#DC3545" Foreground="White" Padding="5,2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>