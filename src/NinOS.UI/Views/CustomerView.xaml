<UserControl x:Class="NinOS.UI.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="wrapped_text_style" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F4F4F4" CornerRadius="4" Margin="0,0,0,10" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Buscar Cliente:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBox Width="300" Text="{Binding search_query, UpdateSourceTrigger=PropertyChanged}" Padding="6" FontSize="14" />
                </StackPanel>

                <Button Grid.Column="2" Content="+ Añadir Cliente" Command="{Binding open_add_window_command}" Padding="20,8" Background="#0078D7" Foreground="White" FontWeight="Bold" Cursor="Hand" />
            </Grid>
        </Border>

        <TabControl Grid.Row="1" SelectedIndex="{Binding selected_tab_index, UpdateSourceTrigger=PropertyChanged}">
            <TabItem Header="Todos">
                <DataGrid ItemsSource="{Binding all_customers}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F9F9F9" MinRowHeight="42" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding id_display}" Width="50" />
                        <DataGridTextColumn Header="Código" Binding="{Binding customer_code}" Width="110" />
                        <DataGridTextColumn Header="Razón Social" Binding="{Binding business_name}" Width="220" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="RIF" Binding="{Binding rif}" Width="130" />
                        <DataGridTextColumn Header="Contacto" Binding="{Binding contact_name}" Width="140" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding phone_number}" Width="140" />
                        <DataGridTextColumn Header="Dirección Fiscal" Binding="{Binding fiscal_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Dirección Entrega" Binding="{Binding delivery_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Vendedor" Binding="{Binding seller_name}" Width="95" />
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Editar"
                                            Command="{Binding DataContext.edit_customer_command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5"
                                            Background="#E0E0E0"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Juan">
                <DataGrid ItemsSource="{Binding juan_customers}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F9F9F9" MinRowHeight="42" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding id_display}" Width="50" />
                        <DataGridTextColumn Header="Código" Binding="{Binding customer_code}" Width="110" />
                        <DataGridTextColumn Header="Razón Social" Binding="{Binding business_name}" Width="220" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="RIF" Binding="{Binding rif}" Width="130" />
                        <DataGridTextColumn Header="Contacto" Binding="{Binding contact_name}" Width="140" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding phone_number}" Width="140" />
                        <DataGridTextColumn Header="Dirección Fiscal" Binding="{Binding fiscal_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Dirección Entrega" Binding="{Binding delivery_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Editar"
                                            Command="{Binding DataContext.edit_customer_command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5"
                                            Background="#E0E0E0"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Sandra">
                <DataGrid ItemsSource="{Binding sandra_customers}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F9F9F9" MinRowHeight="42" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding id_display}" Width="50" />
                        <DataGridTextColumn Header="Código" Binding="{Binding customer_code}" Width="110" />
                        <DataGridTextColumn Header="Razón Social" Binding="{Binding business_name}" Width="220" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="RIF" Binding="{Binding rif}" Width="130" />
                        <DataGridTextColumn Header="Contacto" Binding="{Binding contact_name}" Width="140" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding phone_number}" Width="140" />
                        <DataGridTextColumn Header="Dirección Fiscal" Binding="{Binding fiscal_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Dirección Entrega" Binding="{Binding delivery_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Editar"
                                            Command="{Binding DataContext.edit_customer_command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5"
                                            Background="#E0E0E0"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Anais">
                <DataGrid ItemsSource="{Binding anais_customers}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F9F9F9" MinRowHeight="42" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding id_display}" Width="50" />
                        <DataGridTextColumn Header="Código" Binding="{Binding customer_code}" Width="110" />
                        <DataGridTextColumn Header="Razón Social" Binding="{Binding business_name}" Width="220" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="RIF" Binding="{Binding rif}" Width="130" />
                        <DataGridTextColumn Header="Contacto" Binding="{Binding contact_name}" Width="140" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding phone_number}" Width="140" />
                        <DataGridTextColumn Header="Dirección Fiscal" Binding="{Binding fiscal_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTextColumn Header="Dirección Entrega" Binding="{Binding delivery_address}" Width="260" ElementStyle="{StaticResource wrapped_text_style}" />
                        <DataGridTemplateColumn Header="Acciones" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Editar"
                                            Command="{Binding DataContext.edit_customer_command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5"
                                            Background="#E0E0E0"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>