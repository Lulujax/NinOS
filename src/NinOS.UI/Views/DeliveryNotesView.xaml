<UserControl x:Class="NinOS.UI.Views.DeliveryNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{Binding note_accent_color}" BorderThickness="1" Padding="8" Margin="0,0,0,8" Background="{Binding note_soft_color}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Vendedora:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding sellers}" SelectedItem="{Binding selected_seller, Mode=TwoWay}" DisplayMemberPath="full_name" Width="180" Margin="0,0,30,0"/>
                <TextBlock Text="Tipo de nota:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding note_type_options}" SelectedItem="{Binding selected_note_type, Mode=TwoWay}" DisplayMemberPath="name" Width="140"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding has_selection}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <Border BorderBrush="Black" BorderThickness="1" Padding="10" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="{Binding header_title}" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" TextDecorations="Underline"/>
                        <TextBlock Text="Caracas venezuela" FontSize="14" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock FontStyle="Italic" TextAlignment="Center"><Run Text="RAZON SOCIAL"/><Run Text=" *" Foreground="Red"/></TextBlock></Border>
                            <Border Grid.Row="0" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2">
                                <ComboBox ItemsSource="{Binding filtered_customers}" SelectedItem="{Binding selected_customer}" DisplayMemberPath="business_name" IsEditable="True" IsTextSearchEnabled="True" TextSearch.TextPath="business_name" BorderThickness="0" Background="Transparent"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="Fecha Emision" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="0" Grid.Column="3" BorderBrush="Black" BorderThickness="0,0,0,1" Padding="2">
                                <DatePicker SelectedDate="{Binding creation_date, ValidatesOnExceptions=True}" BorderThickness="0" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="NOMBRE COMERCIAL" FontStyle="Italic" TextAlignment="Center" TextWrapping="Wrap"/></Border>
                            <Border Grid.Row="1" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="{Binding selected_customer.business_name}" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border>
                            <Border Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2">
                                <TextBox Text="{Binding credit_days_text, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" TextAlignment="Center" VerticalContentAlignment="Center" FontSize="13" Padding="4"/>
                            </Border>

                            <Border Grid.Row="2" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="DOMICILIO FISCAL" FontStyle="Italic" TextAlignment="Center" TextWrapping="Wrap"/></Border>
                            <Border Grid.Row="2" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="{Binding selected_customer.fiscal_address}" TextWrapping="Wrap" TextAlignment="Center"/></Border>
                            <Border Grid.Row="2" Grid.Column="2" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="Fecha Vencimiento" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="2" Grid.Column="3" BorderBrush="Black" BorderThickness="0,0,0,1" Padding="2">
                                <DatePicker SelectedDate="{Binding due_date, ValidatesOnExceptions=True}" BorderThickness="0" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="3" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="DIRECCION DE ENTREGA" FontStyle="Italic" TextAlignment="Center" TextWrapping="Wrap"/></Border>
                            <Border Grid.Row="3" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="{Binding selected_customer.effective_delivery_address}" TextWrapping="Wrap" TextAlignment="Center"/></Border>
                            <Border Grid.Row="3" Grid.Column="2" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock TextAlignment="Center"><Run Text="{Binding document_label, Mode=OneWay}"/><Run Text=" NRO"/></TextBlock></Border>
                            <Border Grid.Row="3" Grid.Column="3" BorderBrush="Black" BorderThickness="0,0,0,1" Padding="2"><TextBlock Text="{Binding note_number}" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border>

                            <Border Grid.Row="4" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="RIF:" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="4" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="{Binding selected_customer.rif}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/></Border>
                            <Border Grid.Row="4" Grid.Column="2" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="2"><TextBlock Text="codigo" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="4" Grid.Column="3" BorderBrush="Black" BorderThickness="0,0,0,1" Padding="2">
                                <TextBox Text="{Binding customer_code_text, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" TextAlignment="Center" VerticalContentAlignment="Center" FontWeight="Bold"/>
                            </Border>

                            <Border Grid.Row="5" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,0" Padding="2"><TextBlock Text="TELEFONO:" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="5" Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,0" Padding="2"><TextBlock Text="{Binding selected_customer.phone_number}" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border>
                            <Border Grid.Row="5" Grid.Column="2" BorderBrush="Black" BorderThickness="0,0,1,0" Padding="2"><TextBlock Text="contacto" FontStyle="Italic" TextAlignment="Center"/></Border>
                            <Border Grid.Row="5" Grid.Column="3" BorderBrush="Black" BorderThickness="0,0,0,0" Padding="2">
                                <TextBox Text="{Binding contact_name_text, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" TextAlignment="Center" VerticalContentAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding note_details}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" 
                              GridLinesVisibility="All" HorizontalGridLinesBrush="Black" VerticalGridLinesBrush="Black" BorderBrush="Black" BorderThickness="1,1,1,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="cantidad" Binding="{Binding quantity, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontStyle" Value="Italic"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTemplateColumn Header="Codigo" Width="100">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontStyle" Value="Italic"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="22"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0" Text="{Binding item_search_code, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" Padding="2" VerticalContentAlignment="Center"/>
                                            <Button Grid.Column="1" Content="&#x25BC;" FontSize="9" Foreground="#555555" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" ToolTip="Ver todos" Command="{Binding toggle_popup_command}" CommandParameter="code">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="Transparent" Padding="0,0,2,0">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#E0E0E0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            <Popup Grid.ColumnSpan="2" IsOpen="{Binding show_search_popup_code, Mode=TwoWay}" Placement="Bottom" StaysOpen="False" AllowsTransparency="False" PopupAnimation="Fade">
                                                <Border Background="White" BorderBrush="#888888" BorderThickness="1" MaxHeight="200" MinWidth="560" CornerRadius="2" Padding="2">
                                                    <ListBox x:Name="CodeListBox" ItemsSource="{Binding available_items}" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" Background="White">
                                                        <ListBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Padding="4,3" Cursor="Hand" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Background="White">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Style.Triggers>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                                </Trigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <Border.InputBindings>
                                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.select_item_command, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
                                                                    </Border.InputBindings>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding code}" FontWeight="Bold" FontSize="11" Width="80" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding name}" FontSize="11" Width="330" TextTrimming="CharacterEllipsis" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding unit_price_usd, StringFormat=N2}" FontSize="11" Width="50" TextAlignment="Right" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding available_stock}" FontSize="11" Width="35" TextAlignment="Right" FontWeight="Bold" Foreground="Black"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ListBox.ItemTemplate>
                                                    </ListBox>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="Descripcion" Width="*" MinWidth="380">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontStyle" Value="Italic"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="22"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0" Text="{Binding item_search_text, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" Padding="2" VerticalContentAlignment="Center"/>
                                            <Button Grid.Column="1" Content="&#x25BC;" FontSize="9" Foreground="#555555" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" ToolTip="Ver todos" Command="{Binding toggle_popup_command}" CommandParameter="text">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="Transparent" Padding="0,0,2,0">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#E0E0E0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            <Popup Grid.ColumnSpan="2" IsOpen="{Binding show_search_popup_text, Mode=TwoWay}" Placement="Bottom" StaysOpen="False" AllowsTransparency="False" PopupAnimation="Fade">
                                                <Border Background="White" BorderBrush="#888888" BorderThickness="1" MaxHeight="200" MinWidth="560" CornerRadius="2" Padding="2">
                                                    <ListBox x:Name="DescListBox" ItemsSource="{Binding available_items}" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" Background="White">
                                                        <ListBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Padding="4,3" Cursor="Hand" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Background="White">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Style.Triggers>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                                </Trigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <Border.InputBindings>
                                                                        <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.select_item_command, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
                                                                    </Border.InputBindings>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding code}" FontWeight="Bold" FontSize="11" Width="80" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding name}" FontSize="11" Width="330" TextTrimming="CharacterEllipsis" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding unit_price_usd, StringFormat=N2}" FontSize="11" Width="50" TextAlignment="Right" Foreground="Black"/>
                                                                        <TextBlock Text="{Binding available_stock}" FontSize="11" Width="35" TextAlignment="Right" FontWeight="Bold" Foreground="Black"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ListBox.ItemTemplate>
                                                    </ListBox>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTextColumn Header="precio unitario" Binding="{Binding unit_price_usd, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="100">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontStyle" Value="Italic"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTextColumn Header="precio promocion" Binding="{Binding promo_price_usd_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="120">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontWeight" Value="Bold"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="TOTAL" Binding="{Binding subtotal_usd, StringFormat=N2}" IsReadOnly="True" Width="100">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader"><Setter Property="FontStyle" Value="Italic"/><Setter Property="HorizontalContentAlignment" Value="Center"/></Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTemplateColumn Header="X" Width="30">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="-" Background="#FFCDD2" Command="{Binding DataContext.remove_item_command, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Cursor="Hand" BorderThickness="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="3" Content="+ Agregar Producto" Command="{Binding add_item_command}" HorizontalAlignment="Left" Background="#E0E0E0" Padding="10,2" Margin="0,5,0,0" Cursor="Hand"/>

                    <Border Grid.Row="4" BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                                                    <TextBox Text="{Binding conditions_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" TextWrapping="Wrap" AcceptsReturn="True" FontStyle="Italic" FontWeight="Bold" BorderThickness="0,0,1,0" BorderBrush="Black" Padding="5"/>
                                                    <TextBlock Text="*" Foreground="Red" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,2,0" FontWeight="Bold"/>
                                                </Grid>
                            <TextBlock Grid.Column="1" Text="Total General" FontStyle="Italic" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1,0,0,0">
                                <TextBlock Text="{Binding gross_total_usd, StringFormat=N2}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border Grid.Row="5" BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="{Binding note_payment_bg}" BorderBrush="Black" BorderThickness="0,0,0,1" Padding="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding payment_header_label}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding payment_transfer_label}" TextDecorations="Underline" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding payment_account_label}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="PAGO MOVIL" TextDecorations="Underline" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding payment_movil_phone_label}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="{Binding note_payment_bg}" BorderBrush="Black" BorderThickness="0,0,1,0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBox Text="{Binding discount_conditions_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="Black" TextWrapping="Wrap" AcceptsReturn="True" Padding="2" TextAlignment="Center" FontWeight="Bold"/>
                                            <TextBlock Text="*" Foreground="Red" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,2,0" FontWeight="Bold"/>
                                        </Grid>
                                        
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1"><TextBlock Text="sub total" TextAlignment="Center" Padding="2"/></Border>
                                            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,0,1"><TextBlock Text="{Binding gross_total_usd, StringFormat=N2}" FontStyle="Italic" FontWeight="Bold" TextAlignment="Center" Padding="2"/></Border>
                                        </Grid>

                                        <Grid Grid.Row="2" Visibility="{Binding has_promo_discount, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="5">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBox Text="{Binding promo_discount_percentage_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="60" Background="White" BorderThickness="1" BorderBrush="#AAAAAA" TextAlignment="Center" MaxLength="6" PreviewTextInput="OnDiscountPercentPreview"/>
                                                    <TextBlock Text="% PROMO" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Border>
                                            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,0,1"><TextBlock Text="{Binding promo_discount_amount, StringFormat=N2}" FontWeight="Bold" TextAlignment="Center" Padding="2" VerticalAlignment="Center"/></Border>
                                        </Grid>

                                        <Grid Grid.Row="3">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="5">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBox Text="{Binding discount_percentage_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="60" Background="White" BorderThickness="1" BorderBrush="#AAAAAA" TextAlignment="Center" MaxLength="6" PreviewTextInput="OnDiscountPercentPreview"/>
                                                    <TextBlock Text="%" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Border>
                                            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,0,1"><TextBlock Text="{Binding discount_amount, StringFormat=N2}" FontWeight="Bold" TextAlignment="Center" Padding="2" VerticalAlignment="Center"/></Border>
                                        </Grid>

                                        <Grid Grid.Row="4">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,0"><TextBlock Text="Total General" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap" Padding="2"/></Border>
                                            <Border Grid.Column="1"><TextBlock Text="{Binding discounted_total_usd, StringFormat=N2}" FontWeight="Bold" TextAlignment="Center" Padding="2" VerticalAlignment="Center"/></Border>
                                        </Grid>
                                    </Grid>
                                </Border>

                                <Border Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,1,0">
                                    <TextBlock Text="FECHA _ FIRMA Y SELLO DEL CLIENTE" FontStyle="Italic" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                                </Border>

                                <StackPanel Grid.Column="2" Margin="10" VerticalAlignment="Bottom">
                                    <TextBlock Text="fecha de pago: ________________" Margin="0,0,0,5"/>
                                    <TextBlock Text="monto Bs: ____________________" Margin="0,0,0,5"/>
                                    <TextBlock Text="Nro Referencia: ________________" Margin="0,0,0,15"/>
                                    <TextBlock Text="Banco: _______________________"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                    
                    <Border Grid.Row="6" BorderBrush="Black" BorderThickness="1" Width="460" HorizontalAlignment="Left" Margin="0,10,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,1" Padding="6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="Descuento por volumen" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                                        <TextBox Text="{Binding volume_discount_percentage_text, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="70" Margin="10,0,0,0" Background="White" BorderThickness="1" BorderBrush="#AAAAAA" TextAlignment="Center" MaxLength="6" PreviewTextInput="OnDiscountPercentPreview"/>
                                        <TextBlock Text="%" FontWeight="Bold" FontSize="13" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" BorderBrush="Black" BorderThickness="0,0,0,1"><TextBlock Text="{Binding volume_discount_amount, StringFormat=N2}" FontWeight="Bold" FontSize="13" TextAlignment="Center" Padding="6" VerticalAlignment="Center"/></Border>
                            </Grid>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,1,0"><TextBlock Text="TOTAL A PAGAR" FontWeight="Bold" FontSize="14" TextAlignment="Center" Padding="6" TextWrapping="Wrap"/></Border>
                                <Border Grid.Column="1"><TextBlock Text="{Binding total_amount_usd, StringFormat=N2}" FontWeight="Bold" FontSize="14" TextAlignment="Center" Padding="6" VerticalAlignment="Center"/></Border>
                            </Grid>
                        </Grid>
                    </Border>
                    
                    <Button Grid.Row="7" Content="GUARDAR NOTA" Command="{Binding save_note_command}" Background="{Binding note_guardar_color}" Foreground="White" FontWeight="Bold" FontSize="16" Padding="20,10" Margin="0,20,0,0" Cursor="Hand" HorizontalAlignment="Right"/>

                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>